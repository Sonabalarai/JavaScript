// ?(re-assigned garna mildina but can be change)constant=>which cannot be re-assigned i.e same name varaible with equal sign(=) non-primitive data types(object[with key can be change] and array[with index]).
// ?but constant in primitive data types(numbers,strings etc) it cannot be changed(or re-assigned)

// ?constant cannot be re-assigned(=)
// ?let ma it can be change

//? var why? not use due to hoisting reason(i.e pull up)
// ?e.g
console.log(a)
var a=5 //undefined it is due to hoisting which cannot be performed by let and const

// ?hoisting below gare dinxa  as scnerio down
var a=5//hoisting done by computer it self
console.log(a)
